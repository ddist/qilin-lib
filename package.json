{
  "name": "qilin-sdk",
  "version": "1.0.0",
  "description": "A Typescript library that provides a high level interface to the Qilin Engine",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "npx tsc",
    "prepare": "npm run test && npm run build",
    "test": "jest --coverage",
    "docs": "npx typedoc --out docs ./src && cp ./_config.yml ./docs/_config.yml"
  },
  "author": "Diego Perez <diego@agile.cl>",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.21",
    "codecov": "^3.6.1",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typedoc": "^0.15.0",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
